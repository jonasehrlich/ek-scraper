[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ek-scraper"
version = "0.0.1"
dependencies = [
    "aiohttp<4",
    "beautifulsoup4",
    "lxml<5",
]

[project.scripts]
ek-scraper = "ek_scraper.cli:main"
