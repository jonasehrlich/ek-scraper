[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ek-crawler"
version = "0.0.1"
dependencies = [
    "aiohttp<4",
    "beautifulsoup4",
    "lxml<5",
]

[project.scripts]
ek-crawler = "ek_crawler.cli:main"
